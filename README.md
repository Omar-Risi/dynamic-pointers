# Dynamic Array
This project was created so I could practice memory management 
using c++. 

# Summary
Since c++ has set length for an array defined normally in this way
`int intArr[10];` I have created a custom class to automatically increase
it's size depending on big we would want the array to be.


## Note: the std namespace already has this feature with std::martix

